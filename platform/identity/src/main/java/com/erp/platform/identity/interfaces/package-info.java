/**
 * Interface layer for the identity bounded context.
 *
 * <p>Contains REST controllers and other interface adapters that handle
 * incoming requests and translate them into application service calls.
 *
 * @since 1.0.0
 */
package com.erp.platform.identity.interfaces;
