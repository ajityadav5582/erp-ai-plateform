/**
 * REST controllers for the identity bounded context.
 *
 * <p>Exposes HTTP endpoints for user, role, and permission management,
 * following the platform API standards for versioning, status codes,
 * and error handling.
 *
 * @since 1.0.0
 */
package com.erp.platform.identity.interfaces.rest;
